import './style.css'
import { add, subtract } from './utils';

const firstname: string = "Mumin";

console.log(firstname);

const a = add(5, 6);
console.log(a);

const s = subtract(30, 7);
console.log(s);